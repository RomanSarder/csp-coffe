export enum BufferType {
    FIXED = 'FIXED',
    SLIDING = 'SLIDING',
    DROPPING = 'DROPPING',
    UNBLOCKING = 'UNBLOCKING',
    CLOSED = 'CLOSED',
}

export enum CreatableBufferType {
    FIXED = 'FIXED',
    SLIDING = 'SLIDING',
    DROPPING = 'DROPPING',
    UNBLOCKING = 'UNBLOCKING',
}
